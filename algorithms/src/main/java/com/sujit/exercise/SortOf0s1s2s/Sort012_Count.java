package com.sujit.exercise.SortOf0s1s2s;

/**
 * Algorithm to sort an array consisting of only 0's, 1's and 2's.
 *
 * Time Complexity: O(n)
 *
 * Count the number of 0s, 1s and 2s in the given array.
 * Then store all the 0s in the beginning followed by
 * all the 1s then all the 2s.
 */
public class Sort012_Count {
}
